/**
 * @file Speculative file for storing database type definitions. Might not be
 * needed, depending on what features Prisma has.
 */
import { CardJson } from "../clientJsonTypes";

/** This is only temporary. The types will likely end up a little different. */
export type CardDatabaseJson = CardJson;
